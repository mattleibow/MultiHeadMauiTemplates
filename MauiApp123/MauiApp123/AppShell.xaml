<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp123.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp123"
    xmlns:pages="clr-namespace:MauiApp123.Pages"
    Title="MauiApp123"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title=".NET MAUI">
            <ShellContent
                Title=".NET MAUI"
                ContentTemplate="{DataTemplate pages:MainPage}"
                Route="Main" />
        </Tab>
        <Tab Title=".NET MAUI + Platform">
            <ShellContent
                Title=".NET MAUI + Platform"
                ContentTemplate="{DataTemplate pages:PlatformsPage}"
                Route="Platforms" />
        </Tab>
    </TabBar>

</Shell>
